{
    "scripts": {
        "build": "jupyter nbconvert presentation.ipynb --to slides --SlidesExporter.reveal_scroll=True --TagRemovePreprocessor.remove_input_tags={\"remove-input\"}",
        "predeploy": "mkdir -p build && cp presentation.slides.html build/index.html",
        "deploy": "npx gh-pages -d build"
    }
}